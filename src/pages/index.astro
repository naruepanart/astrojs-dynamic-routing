---
import Layout from "../layouts/Layout.astro";

const response = await fetch("https://jsonplaceholder.typicode.com/posts");
const posts = await response.json();
---

<Layout title="AstroJS Dynamic Page">
  <h1>AstroJS Dynamic Page</h1>
  <main>
    {
      posts.map((x: any) => (
        <div>
          <a href={`${x.id}`}>
            {x.id} - {x.title}
          </a>
        </div>
      ))
    }
  </main>
</Layout>
