---
import Layout from "../layouts/Layout.astro";

const response = await fetch("https://jsonplaceholder.typicode.com/posts");
const posts = await response.json();
---

<Layout title="Astro Dynamic">
  <h1>Astro Dynamic Page</h1>
  <main>
    {
      posts.map((x: any) => (
        <div>
          <a href={`${x.id}`}>
            {x.id} - {x.title}
          </a>
        </div>
      ))
    }
  </main>
</Layout>
